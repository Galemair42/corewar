#include "corewar.h"

void cw_inst_st(t_processus *process)
{
    int     param_2;

    /*if ((process->ocp >> 4) & 3 == 1) // pour isoler les deux bits de l'ocp qui minteresse
    {
        process->reg[process->params[1]] = process->reg[process->params[0]];
    }
    if ((process->ocp >> 4) & 3 == 3)
    {
        param_2 = (process->reg[process->params[1]] + process->pc) & 0xFFF;
        arena.memory[param_2];
    }
    process->pc = (process->pc + ret) & 0xFFF; // c'est ok ca */



    // a refare c de la merde ca !!!
}